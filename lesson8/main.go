package main

// func main() {
	// var s1, s2 = "Hello", "hello"
	// n := strings.Compare(s1, s2)
	// fmt.Println(n)
	// // strings.Clone()
	// s := "оафошоjisoadаввы"
	// for i, v := range s {
	// 	fmt.Println(i, v)
	// }
	// fmt.Println(s, len(s))

	// age := 19
	// var err error
	// if age < 18 {
	// 	err = fmt.Errorf("siz %d yoshdasiz. Saylovda qatnashish uchun kamida 18 yosh bo'lishiz kerak", age)
	// }

	// if err != nil {
	// 	fmt.Println("No permission")
	// 	return
	// }
	// year := 1984
	// yearS := strconv.Itoa(year)

	// fmt.Println(yearS)
	// yearS = "1984"
	// y, err := strconv.Atoi(yearS)
	// if err != nil {
	// 	panic(err)
	// }

	// fmt.Println(y)

	// b := []byte{}
	// fmt.Println(strconv.AppendBool(b, false))
	// i, err := strconv.ParseInt("F", 16, 16)
	// if err != nil {
	// 	panic(err)
	// }
	// fmt.Println(i)
	// t r u e
	// f a l s e

	// fmt.Println(time.Now())

	// date := time.Date(2025, 12, 11, 23, 43, 12, 12, time.Local)
	// fmt.Println(date)
	// // month day,  hour minute second   year
	// // 1,    2,     23,  4,     5,       6,
	// t := date.Format(time.Layout)

	// t1, err := time.Parse(time.Layout, t)
	// if err != nil {
	// 	panic(err)
	// }
	// fmt.Println(t1, t1.Weekday(), t1.Year(), t1.Second(), t1.Day(), time.Second)
	// t1 = t1.Add(3 * time.Hour)
	// fmt.Println(t1)

	// fmt.Printf("")
	// s := fmt.Sprintf("%v %d\n", time.Now(), 34215)
	// time.Sleep(2 * time.Second)

	// fmt.Println(s, time.Now().Format("01/02 15:04:05 Yil:2006 -07"))

// }
